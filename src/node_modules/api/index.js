import recentActivity from "./fakeData";
// import Request from 'superagent'

console.log('getting data..',recentActivity)

export const fetchActivity = async () => {
        console.log('fetch activies api...  ')
        // can be replaced with Request Axios or superagent
        return new Promise(resolve => resolve(recentActivity))
  }

